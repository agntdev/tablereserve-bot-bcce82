[
  {
    "name": "/start replies with the welcome + main menu",
    "steps": [
      {
        "send": { "text": "/start" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Welcome to TableReserve!\n\nBook a table in a few taps, check your reservation, or manage the restaurant if you're the owner."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "Back to menu re-renders the welcome",
    "steps": [
      {
        "send": { "text": "/start" },
        "expect": [{ "method": "sendMessage" }]
      },
      {
        "send": { "callback": "menu:main" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Welcome to TableReserve!\n\nBook a table in a few taps, check your reservation, or manage the restaurant if you're the owner."
            }
          }
        ]
      }
    ]
  }
]
